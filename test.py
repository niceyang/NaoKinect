import numpy as np

# list = []
# print 'dd'
# for i in range(19):
# 	list.append(i)
# 	print "*",i
# list[110]=999
# print list[110]=999

a = np.array([[1,2],[3,4]])
b1= np.array([2,4,6])
b2= np.array([3,3])

print "Gudan"
print b1[2]

# print a.dot(b2-b1)
# print a.dot(b2)-a.dot(b1)



        # # Head
        # ohead = np.array([skeleton.joints[3].x, skeleton.joints[3].y, skeleton.joints[3].z])
        # oneck = np.array([skeleton.joints[2].x, skeleton.joints[2].y, skeleton.joints[2].z])
        # # Right
        # oright_shoulder = np.array([skeleton.joints[8].x, skeleton.joints[8].y, skeleton.joints[8].z])
        # oright_elbow = np.array([skeleton.joints[9].x, skeleton.joints[9].y, skeleton.joints[9].z])
        # oright_wrist = np.array([skeleton.joints[10].x, skeleton.joints[10].y, skeleton.joints[10].z])
        # # Left 
        # oleft_shoulder = np.array([skeleton.joints[4].x, skeleton.joints[4].y, skeleton.joints[4].z])
        # oleft_elbow = np.array([skeleton.joints[5].x, skeleton.joints[5].y, skeleton.joints[5].z])
        # oleft_wrist = np.array([skeleton.joints[6].x, skeleton.joints[6].y, skeleton.joints[6].z])
        # # Legs
        # oleft_hip = np.array([skeleton.joints[12].x, skeleton.joints[12].y, skeleton.joints[12].z])
        # oright_hip = np.array([skeleton.joints[16].x, skeleton.joints[16].y, skeleton.joints[16].z])
        # oleft_knee = np.array([skeleton.joints[13].x, skeleton.joints[13].y, skeleton.joints[13].z])
        # oright_knee = np.array([skeleton.joints[17].x, skeleton.joints[17].y, skeleton.joints[17].z])


        # # Head
        # head = A.dot(ohead) 
        # neck = A.dot(oneck) 
        # # Right
        # right_shoulder = A.dot(oright_shoulder)
        # right_elbow = A.dot(oright_elbow)
        # right_wrist = A.dot(oright_wrist)
        # # Left 
        # left_shoulder = A.dot(oleft_shoulder)
        # left_elbow = A.dot(oleft_elbow)
        # left_wrist = A.dot(oleft_wrist) 
        # # Legs
        # left_hip =  A.dot(oleft_hip)
        # right_hip = A.dot(oright_hip) 
        # left_knee = A.dot(oleft_knee) 
        # right_knee = A.dot(oright_knee)
